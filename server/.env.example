PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/geofit
# Dual engine fallback: If DATABASE_URL is unavailable or fails, automatically falls back to local SQLite in dev
USE_SQLITE_FALLBACK=true
JWT_SECRET=geofit_super_secret_cyber_jwt_token_2026
